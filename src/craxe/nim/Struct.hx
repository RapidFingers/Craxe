package craxe.nim;

/**
 * Nim's value object:
 * 1. It can't be null
 * 2. It can't be inherited
 * 3. It passed by value, but can be passed by reference with "var" modifier
 */
extern class Struct {}