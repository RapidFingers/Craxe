package craxe.nim;

/**
 * Structure is a value object:
 * 1. It can't be null
 * 2. It can't be inherited
 * 3. It passed by value, but can be modified if they passed with "var" modifier
 */
extern class Struct {}